import { Component } from 'react';

import { type CreatableProps, defaultProps } from 'react-select-oss/src/Creatable';

export default class Select extends Component<CreatableProps> {
  defaultProps = defaultProps;
}
