import { Component } from 'react';

import { type AsyncProps, defaultProps } from 'react-select-oss/src/Async';

export default class Select extends Component<AsyncProps> {
  defaultProps = defaultProps;
}
